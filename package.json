{
  "name": "notes-app",
  "version": "0.1.0",
  "homepage": "https://arne-slabbinck.github.io/notes-app",
  "devDependencies": {
    "gh-pages": "^6.1.0"
  },
  "scripts":{
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}
